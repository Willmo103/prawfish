class Prawfish(no_persist=True, ):
    # conditional for kw: no_persist = True
    # set to False as default, no need to setup a database connection if i dont need to

        # run a query in database for config load config
        # do a json search for the data
    pass
